{
  "name": "claude-defender-xdr",
  "version": "1.2.2",
  "description": "Claude Code plugin for Microsoft Defender XDR Advanced Hunting",
  "type": "module",
  "license": "MIT",
  "author": "Andrew Childs",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/server.js --banner:js=\"// Generated by npm run build. Do not edit.\"",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run check && npm test && npm run build && npm run check:dist && npm run check:plugin",
    "check:dist": "git diff --exit-code -- dist || node -e \"console.error('dist/ is stale: run npm run build and commit dist/'); process.exit(1)\"",
    "check:plugin": "claude plugin validate .",
    "package": "node scripts/package-plugin.mjs",
    "check:package": "npm run package && claude plugin validate build/defender-xdr",
    "schema": "node scripts/update-schema-snapshot.mjs",
    "schema:check": "node scripts/update-schema-snapshot.mjs --check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@types/node": "24.0.15",
    "esbuild": "0.25.10",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.25.10": true
  }
}
