node_modules/
.env
*.log
coverage/

# dist/ is intentionally committed: Claude Code installs plugins by cloning, with no
# npm install or build step, so the compiled server must be present in the repository.
# `npm run verify` fails if it drifts from server/.
