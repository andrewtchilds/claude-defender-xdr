#!/bin/sh
# Claude Code adds this plugin's bin/ directory to the Bash tool's PATH while the
# plugin is enabled, so /defender-xdr:xdr-login resolves without a global npm install.
set -e
here=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec node "$here/../scripts/xdr-login.mjs" "$@"
